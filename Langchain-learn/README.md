# Langchain Learn

这个目录用于通过 Jupyter Notebook 分步学习和调试 LangChain/Agent 开发流程。

## 启动 Notebook

当前 Windows 环境无法写入默认的 Jupyter 用户目录和内核连接文件 ACL，所以请用项目内启动脚本：

```powershell
.\start-notebook.ps1
```

如果只需要安装依赖：

```powershell
uv add notebook
```
